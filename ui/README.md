OS Forensics — UI
=================

This folder contains a minimal React UI scaffold (Vite).

Quickstart:

```bash
cd ui
npm install
npm run dev
```

The UI will run on the port Vite chooses (usually 5173). Update the UI to
call the backend `/analyze` endpoint at `http://127.0.0.1:8000/analyze`.
